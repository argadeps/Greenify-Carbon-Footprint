// client/interfaces/UserData.tsx

interface User {
  id: number | null;
  username: string | null;
  email: string | null;
  // createdAt: string;?
}

export default User; // This is a default export
